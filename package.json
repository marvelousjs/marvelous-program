{
  "name": "@marvelousjs/program",
  "version": "0.4.0",
  "description": "the marvelous program",
  "main": "./dist/app.js",
  "dependencies": {
    "chalk": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "typescript": "^3.2.1"
  },
  "files": [
    "bin/",
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "test": "NODE_ENV=test mocha -r ts-node/register src/**/*.spec.ts",
    "watch": "tsc -w"
  }
}
